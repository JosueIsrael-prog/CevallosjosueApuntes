<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="CevallosjosueApuntes.Views.NotePage"
             Title="Nota">
    <VerticalStackLayout>
        <Editor Text="{Binding Text}" Placeholder="Escribe tu nota aquí..." />
        <Button Text="Guardar"
                BackgroundColor="Purple"
                TextColor="White"
                Clicked="OnSaveClicked" />
    </VerticalStackLayout>
</ContentPage>
